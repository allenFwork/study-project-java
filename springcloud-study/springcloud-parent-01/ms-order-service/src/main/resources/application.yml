server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://192.168.80.4:3306/cloud_order?useUnicode=true&characterEncoding=UTF-8&autoReconnect=true&serverTimezone=Asia/Shanghai&rewriteBatchedStatements=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456
  application:
    name: ms-order-service
  cloud:
    nacos: # Spring Cloud Alibaba 中以 nacos为 服务注册管理中心 的相关配置
      server-addr: localhost:8848 # nacos注册中心的服务地址 (将其配置到bootstrap.yml文件中，优先加载该信息得到nacos的注册中心地址)
      discovery:
        cluster-name: HZ # 配置该微服务的集群名称
        namespace: 7c738296-85a6-4f09-a92f-f69e5cfaa6e6 # nacos中配置的命名空间，填ID，生成命名空间时的唯一标识
        ephemeral: false # 设置为非临时实例，默认是临时实例

mybatis:
  type-aliases-package: com.study.springcloud.order.pojo
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    com.study.springcloud.order: debug
  pattern:
    dateformat: MM-dd HH:mm:ss:SSS

# 以eureka为注册中心时，配置的信息
#eureka:
#  client:
#    service-url:
#      defaultZone: http://127.0.0.1:10086/eureka


ms-user-service: # 给某个微服务配置负载均衡规则，这里是ms-user-service服务
  ribbon:
    #    NFLoadBalancerRuleClassName: com.netflix.loadbalancer.RandomRule     # 负载均衡规则(随机策略)
    #    NFLoadBalancerRuleClassName: com.netflix.loadbalancer.RoundRobinRule # 负载均衡规则(轮询策略)
    NFLoadBalancerRuleClassName: com.alibaba.cloud.nacos.ribbon.NacosRule # 负载均衡规则(同集群优先策略)

ribbon:
  # Ribbon默认是采用懒加载，即第一次访问时才会去创建LoadBalanceClient，请求时间会很长。
  # 而饥饿加载则会在项目启动时创建，降低第一次访问的耗时
  eager-load:
    enabled: true
    clients: ms-user-service